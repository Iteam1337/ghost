apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: iteam-ghost
  name: iteam-ghost
  namespace: iteam-se
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  storageClassName: microk8s-hostpath
  volumeMode: Filesystem
